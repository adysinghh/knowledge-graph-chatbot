# knowledge-graph-chatbot
A conversational QA chatbot that uses a Knowledge Graph built from a CSV file. The data is ingested into Neo4j (a graph database), enabling semantic search over skills, rates, and freelancer profiles.
